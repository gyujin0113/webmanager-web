/**
 * Config for the AI 안내 위젯 embed script this landing page dogfoods on itself.
 * Kept separate from the JSX so the loader URL / site id aren't hardcoded literals
 * inline in layout.tsx.
 */
export const WIDGET_SCRIPT_SRC = "https://widget.webmanager.co.kr/v1/w.js";
export const WIDGET_SITE_ID = "webmanager";
export const WIDGET_GUIDE_PATH = "/guide.json";
